# راهنمای GUCCI 3X-UI روی Railway

این مخزن، سورس کامل Release رسمی **MHSanaei/3x-ui v3.4.2** را نگه می‌دارد و لایه Railway/GUCCI را بدون تغییر دادن منطق اصلی پنل روی آن قرار می‌دهد.

## مسیرها

- `/` — فقط صفحه معرفی GUCCI، بدون دکمه یا لینک ورود به پنل
- `/gucci/` — ابتدا صفحه معرفی GUCCI همراه دکمه «متوجه شدم → ورود به پنل»؛ بعد از کلیک، پنل اصلی روی همان URL باز می‌شود
- `/sub/{subId}` — لینک سابسکریپشن اصلی و Native پنل سنایی
- `/json/{subId}` و `/clash/{subId}` — خروجی‌های Native در صورت فعال‌سازی
- `/healthz` — Health Check

## اطلاعات اولیه

- Username: `gucci`
- Password: `gucci`

نام کاربری و رمز اولیه `gucci` است. بعد از اولین ورود، تمام تغییرات مدیر، کاربران و تنظیمات روی Volume باقی می‌مانند و پروژه هیچ Inbound یا تنظیم اضافه‌ای را تحمیل نمی‌کند.

## Volume دائمی

**Mount Path را دقیقاً `/GUCCI` قرار دهید.** این مسیر باید Railway Volume واقعی باشد، نه فضای موقت Container.

Entrypoint پوشه‌های زیر را فقط داخل همان Volume نگه می‌دارد:

- `/GUCCI/x-ui` — مسیر مستقیم دیتابیس SQLite، کاربران، inboundها و تمام تنظیمات
- `/GUCCI/cert` — گواهی‌ها
- `/GUCCI/acme` — وضعیت تمدید گواهی
- `/GUCCI/log` — لاگ‌های پنل

هیچ Startup یا Updateای محتوای `/GUCCI` را reset نمی‌کند. Credentials اولیه فقط وقتی ساخته می‌شوند که دیتابیس واقعاً وجود نداشته باشد؛ بنابراین Redeploy، Restart، Rebuild و ارتقای نسخه اطلاعات پنل را پاک یا بازنویسی نمی‌کنند.

## متغیرهای Railway

هیچ متغیر اجباری نیست. تمام پیش‌فرض‌های لازم داخل `Dockerfile.railway` تنظیم شده‌اند. فقط پس از Deploy یک Domain با Target Port برابر `1` بسازید و مسیر `/gucci/` را باز کنید.

## Pin نسخه پنل

پنل طبق درخواست روی Release رسمی **3X-UI v3.4.2** Pin شده و Workflow زمان‌بندی‌شده ارتقای خودکار غیرفعال است؛ بنابراین Sync خودکار دوباره آن را به نسخه جدیدتر برنمی‌گرداند. Workflow دستی فقط SHA رسمی Tag و Pin پنل/Core را بررسی می‌کند.

پیش از اولین Startup نسخه 3.4.2، Entrypoint یک Snapshot تأییدشده و غیرقابل‌بازنویسی از دیتابیس موجود در `/GUCCI/backups/pre-v3.4.2-transition.db` می‌سازد. سپس همان دیتابیس اصلی و همان Volume بدون Reset استفاده می‌شوند.

## محدودیت Railway

دامنه HTTP پنل و تمام لینک‌های سابسکریپشن کار می‌کنند. Railway برای هر Service فقط یک TCP Proxy عمومی و ورودی UDP محدودی دارد؛ بنابراین برای عمومی‌کردن تعداد زیادی inbound با پورت‌های مستقل، باید از fallback روی یک پورت، Relayهای جدا یا VPS استفاده شود. این محدودیت خود Railway است و قابلیت‌های UI اصلی 3X-UI حذف نشده‌اند.


## سازگاری کلاینت‌ها

نسخه Railway عمداً Xray-core `v26.6.27` را با SHA-256 رسمی Pin می‌کند تا Regression نسخه‌های جدید Reality روی Xray، Mihomo/Clash Meta و کلاینت‌های قدیمی‌تر کاهش یابد. برای بازگرداندن معنای سازگار v3.4.2، هر Reality inbound قدیمی که `minClientVer` خالی داشته باشد در Startup به مقدار صریح `1.0.0` تبدیل می‌شود؛ مقدار غیرخالی انتخاب‌شده مدیر هرگز بازنویسی نمی‌شود. برای sing-box و شبکه‌هایی که TCP Proxy یا پورت غیر 443 را محدود می‌کنند، پروفایل WebSocket زیر مسیر عمومی سازگارتر است.


## مسیر سازگاری عمومی

Nginx مسیر `/gucci-connect/ws` را به `127.0.0.1:1235` Forward می‌کند. برای دستگاه‌هایی که Reality ندارند، یک inbound از نوع VLESS + WebSocket + security none روی پورت داخلی `1235` و همین path بسازید؛ لینک بیرونی باید از دامنه Railway، پورت `443` و `security=tls` استفاده کند.

برای اینکه یک Subscription هم Reality سریع و هم fallback عمومی را تحویل دهد، یک Client روی هر دو inbound باید `subId` یکسان داشته باشد. UUIDها می‌توانند متفاوت باقی بمانند. در production پروفایل عمومی HTTPS/443 با `subSortIndex=1` قبل از REALITY با `subSortIndex=2` ارائه می‌شود تا Clientهای بیشتری در اولین انتخاب پاسخ بگیرند؛ هر دو همچنان قابل انتخاب‌اند و Subscription قدیمی حذف نمی‌شود. پاسخ‌های `/sub/`، `/json/` و `/clash/` نیز `no-store` هستند و فاصله Refresh پیشنهادی production یک ساعت است.

## Health Check اتصال

`/healthz` دیگر پاسخ ثابت نیست. Watchdog ابتدا پورت داخلی پنل و سپس پورت‌های معرفی‌شده در `GUCCI_HEALTH_TCP_PORTS` را واقعاً بررسی می‌کند. در production مقدار آن `1234,1235` است. تا وقتی پنل و هر دو endpoint آماده نباشند Health Check موفق نمی‌شود؛ اگر چند بار متوالی از دسترس خارج شوند، x-ui متوقف می‌شود تا Restart Policy خود Railway سرویس را با همان Volume دائمی بازیابی کند.
